# Dependencies & build output (rebuilt inside the image)
node_modules
.next
out
build

# Secrets — never bake env files into the image
.env
.env.*
!.env.example

# Local uploads (live in the media volume on the server)
public/media

# VCS, editor, tooling
.git
.gitignore
.vscode
.idea
.claude
*.md
Dockerfile
docker-compose*.yml
docker

# Tests
tests
test-results
playwright-report
blob-report
coverage
*.tsbuildinfo
